# Changelog

Všechny významné změny projektu jsou zaznamenány v tomto souboru.

## 0.1 — 2026-07-12

- první funkční Telegram → Codex bridge;
- bezpečný Telegram user allowlist a workspace allowlist;
- per-chat Codex relace přes oficiální TypeScript SDK;
- režimy `read-only` a opt-in `workspace-write`;
- příkazy pro stav, workspace, režim, novou relaci a zastavení úlohy;
- systemd šablona, testy, GitHub CI a instalační dokumentace.
